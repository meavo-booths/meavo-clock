# Same Google OAuth client as api/.env (GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Production build uses relative /api paths — no VITE_API_URL needed when served from clock.meavo.app
